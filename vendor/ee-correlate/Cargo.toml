[package]
name = "ee-correlate"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Correlation & analysis over normalized events (spatial-temporal clustering, rollups) for engineering-effects."

[dependencies]
ee-core = { path = "../ee-core" }
chrono.workspace = true
serde.workspace = true

[dev-dependencies]
serde_json.workspace = true
